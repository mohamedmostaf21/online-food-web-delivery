services:
  # Backend Service Configuration
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: online_food_backend
    ports:
      - "5001:5001"  # Maps port 5001 of your machine to port 5001 of the container
    environment:
      - NODE_ENV=development
      - MONGO_URI=${MONGO_URI:-mongodb://mongo:27017/online_food}
      - PORT=5001
    networks:
      - food_network
    depends_on:
      - mongo
    develop:
      watch:
        - action: sync
          path: ./backend
          target: /usr/src/app
          ignore:
            - node_modules/
        - action: rebuild
          path: ./backend/package.json

  # Frontend Service Configuration
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    container_name: online_food_frontend
    ports:
      - "5173:5173"  # Maps port 5173 of your machine to port 5173 of the container
    environment:
      - CHOKIDAR_USEPOLLING=true  # Helps React hot-reload work smoothly inside Docker containers
      - VITE_API_URL=http://backend:5001/api
    depends_on:
      - backend  # Ensures the backend spins up before the frontend starts
    networks:
      - food_network
    develop:
      watch:
        - action: sync
          path: ./frontend
          target: /usr/src/app
          ignore:
            - node_modules/
        - action: rebuild
          path: ./frontend/package.json

  # MongoDB Service
  mongo:
    image: mongo:7
    container_name: online_food_mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: online_food
    networks:
      - food_network

networks:
  food_network:
    driver: bridge

volumes:
  mongo_data:
